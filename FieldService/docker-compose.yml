# ============================================================
# docker compose up — uruchamia cały system jednym poleceniem
# Wymagane: skopiuj .env.example do .env i ustaw wartości
# ============================================================

services:

  # --- Backend API (C# / ASP.NET Core) ---
  api:
    build: .
    ports:
      - "5050:8080"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    environment:
      - ConnectionStrings__Default=Host=postgres;Database=fieldservice;Username=postgres;Password=${POSTGRES_PASSWORD}
      - ASPNETCORE_ENVIRONMENT=Development
      - Jwt__Key=${JWT_KEY}
      - Cors__AllowedOrigins__0=${CORS_ALLOWED_ORIGIN:-http://localhost:5173}

  # --- Baza danych ---
  postgres:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_DB: fieldservice
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      retries: 5

  # --- Cache i kolejki ---
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  # --- Optymalizacja tras ---
  # VROOM potrzebuje OSRM do obliczania realnych tras drogowych.
  # Na start można pominąć (system użyje Haversine z SuggestionService).
  # Odkomentuj gdy będziesz gotowy na realne trasy:
  #
  # vroom:
  #   image: vroomvrp/vroom-docker:v1.14.0
  #   ports:
  #     - "3000:3000"
  #   depends_on: [osrm]
  #   environment:
  #     - VROOM_ROUTER=osrm
  #     - OSRM_URL=http://osrm:5000
  #
  # osrm:
  #   image: osrm/osrm-backend:latest
  #   ports:
  #     - "5001:5000"
  #   volumes:
  #     - ./osrm-data:/data
  #   command: osrm-routed --algorithm mld /data/poland-latest.osrm

  # --- Storage zdjęć protokołów ---
  minio:
    image: minio/minio:latest
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    command: server /data --console-address ":9001"
    volumes:
      - miniodata:/data

volumes:
  pgdata:
  miniodata:
